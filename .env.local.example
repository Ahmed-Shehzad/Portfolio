# Local Environment Variables for GitHub Actions Testing
# Copy this file to .env.local and customize as needed

# Node.js Environment
NODE_ENV=development
CI=true

# GitHub Actions simulation
GITHUB_ACTIONS=true
GITHUB_WORKFLOW=local-test
GITHUB_RUN_ID=123456
GITHUB_RUN_NUMBER=1
GITHUB_SHA=abc123
GITHUB_REF=refs/heads/main
GITHUB_REPOSITORY=Ahmed-Shehzad/Portfolio

# Application specific
NEXT_TELEMETRY_DISABLED=1

# Build configuration
NODE_OPTIONS=--max_old_space_size=4096

# Email Configuration for Contact Form
# =================================

# SMTP Server Configuration
# For Gmail, use smtp.gmail.com:587
# For other providers, check their SMTP settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# Email Credentials
# For Gmail: use your email and app password (not regular password)
# Generate app password: https://support.google.com/accounts/answer/185833
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Contact Email (where form submissions will be sent)
# If not set, defaults to SMTP_USER
CONTACT_EMAIL=your-email@gmail.com

# ==================================
# Instructions for Gmail Setup:
# ==================================
# 1. Go to your Google Account settings
# 2. Enable 2-Factor Authentication
# 3. Go to "App passwords" section
# 4. Generate a new app password for "Mail"
# 5. Use that app password as SMTP_PASS (not your regular Gmail password)
# 6. Set SMTP_USER to your Gmail address
# 7. Set CONTACT_EMAIL to the email where you want to receive contact form submissions

# For other email providers (Outlook, Yahoo, etc.), check their SMTP settings
# and update SMTP_HOST and SMTP_PORT accordingly
